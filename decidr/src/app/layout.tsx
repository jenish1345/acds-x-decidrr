import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Decidr — Your Personal Decision Agent",
  description:
    "Decidr helps student-athletes make the right trade-offs between study, training, and recovery — based on their own goals, constraints, and history.",
  keywords: ["student-athlete", "AI", "decision", "personal assistant", "scheduling"],
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
