{
  "name": "decidr",
  "version": "2.0.0",
  "description": "DECIDR — The negotiation layer between organizations and people.",
  "scripts": {
    "install:all": "npm install --prefix decidr && npm install --prefix acds_platform/acds-platform",
    "dev:personal-ui": "npm run dev --prefix decidr",
    "dev:org-ui": "npm run dev --prefix acds_platform/acds-platform",
    "dev:personal-api": "cd decidr/backend && python -m uvicorn server:app --port 8000 --reload",
    "dev:org-api": "cd acds_platform/backend && python -m uvicorn server:app --port 8001 --reload",
    "dev": "concurrently -n \"PersonalUI,OrgUI,PersonalAPI,OrgAPI\" -c \"blue,cyan,green,yellow\" \"npm:dev:personal-ui\" \"npm:dev:org-ui\" \"npm:dev:personal-api\" \"npm:dev:org-api\"",
    "build": "npm run build --prefix decidr && npm run build --prefix acds_platform/acds-platform",
    "test": "python3 tests/test_end_to_end.py"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
